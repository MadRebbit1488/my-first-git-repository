'use strict';


let l = [1,2,'a','b'];

function filter_list(l) {
  // Return a new array with the strings filtered out
  let arr = [];
  let array = l.filter(num => num === Number());
      
  return array;
}

console.log(filter_list(l));

//
//.map(n => n == Number() ? true : false).push(arr)
/* .let arr = [];
  let array = l.join(',').map(n => n == Number() ? true : false).sort((a, b)=> a - b).split('').push(arr); */












